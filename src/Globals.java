public class Globals {
    public static final int[] TREASURES = {1,2,2,3,3,3,4,5};

    /*public static void init() {
        for (int i = 0; i < N; i++) {
            int min = Integer.MAX_VALUE;

            for (int j = 0; j < N; j++) {
                if (DISTANCES[i][j] < min && i != j) {
                    min = DISTANCES[i][j];
                }
            }

            MIN_DIST[i] = min;
        }
    }*/
}
